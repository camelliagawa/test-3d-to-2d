{
  "name": "stl-to-relief",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Drag & drop a 3D STL and export a half-relief (2.5D) STL for 3D printing.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types scripts/test-relief.ts"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
